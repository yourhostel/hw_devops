replicaCount: 1

image:
  repository: yourhostel/devops-final
  pullPolicy: Always
  tag: 86392b3

imagePullSecrets:
  - name: dockerhub-secret

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false

namespace: python-app

