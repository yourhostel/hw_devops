replicaCount: 1

image:
  repository: yourhostel/devops-final
  pullPolicy: Always
  tag: a99c590

imagePullSecrets:
  - name: dockerhub-secret

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false

namespace: python-app

